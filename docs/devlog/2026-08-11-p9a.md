# P9a · UI 精致化（+ TD-06）· 阶段记录

> 日期：2026-08-11 ｜ 提交：ac5ce83 ｜ 状态：完成

## 交付

- **设计 token 精修**（`styles/variables.css`）：字体栈（system-ui + 中文回退）、3 级阴影（xs/md/lg）、4pt 间距体系（--space-1..6）、radius 6/10/12、tabular-nums 辅助类、全局 :focus-visible 焦点环、--transition、--skeleton-bg（深色补充 shadow-lg/skeleton）
- **项目列表卡片化**（`project-list/index.vue` 重写）：卡片网格（auto-fill minmax 300px）+ 健康分环形（SVG stroke-dasharray 按分数着色，null 显示 —）+ 语言徽章（固定色板 color-mix 弱化，深色自适应）+ 状态点（READY 绿/ANALYZING 黄呼吸/FAILED 红/CREATED 灰）+ 筛选工具栏（搜索 300ms 防抖、语言▾ 从当前页聚合、状态▾、排序▾、卡片/列表视图切换 localStorage）+ 空态三态内联 SVG + 骨架屏（6 占位卡，evo-pulse）+ 卡片入场 stagger（transition-delay 30ms*n）+ hover 抬升
- **App.vue**：导航 active 下划线指示（::after 2px）、header 毛玻璃（backdrop-filter 88% 透明）、logo 字标（Evo+Code 细体）、路由 fade 过渡（Transition mode=out-in）
- **dashboard**：无项目空态 + 图表区条件渲染（避免空 canvas）
- **detail 页**：radius 硬编码 8/10px → --radius-md/--radius-lg 收编
- **TD-06**：vitest 基建（vitest.config.ts + `npm run test`），`request.ts` 拦截器 4 例 + `chat.ts` SSE 解析 8 例

## 验证

| 项 | 结果 |
|---|---|
| eslint + prettier | 通过（仅 3 既有 v-html 警告，非本次引入） |
| vue-tsc + vite build | 通过 |
| vitest | 12/12 通过 |
| 无头渲染冒烟（系统 Edge） | 页面标题/卡片网格/工具栏/视图切换正确；1 个真实项目卡片结构完整（环形 77 分/名称/来源/状态/行数/文件/分析时间）；无 4xx 请求 |

## 边界记录

- 排序▾ 暂提供「最近分析/代码量/创建时间」三项；healthScore 排序需后端白名单补充，P9e 加入（见 p9-design.md §E2）
- 语言选项聚合自当前页项目（size=100 上限沿用），项目超 100 时选项可能不全（设计稿已记录）
- ⋮ 操作按钮为占位（P9b 接入重命名/导出/删除菜单）
