# P5 演化分析

> 2026-08-11 · 阶段目标记录（实施前起草，实施后补全交付/验证）

## 目标

P0-P4 已交付 v0.3（架构分析）。按 README 开发路线，下一阶段是 **v0.4 演化分析**：
消费 git 历史，呈现"代码如何演化"，并给出**风险中心**判断，补上"能回顾"的能力。

范围：analyzer git 统计 → 落库（V004/V007）→ 查询端点 → 前端演化页三图 + 风险中心。
不改动既有扫描/质量/架构链路；FULL 分析自动附带演化统计（与架构 P4b 同位置接入）。

## 拆分（沿用 P4 的 a/b/c 原子提交模式）

| 子阶段 | 内容 | 验收 |
|---|---|---|
| **P5a** | analyzer `/analyze/v1/evolution`：git log 解析 → commits / trend / topFiles / authors + 规则 hotspots；非 git 目录 → 200 + `{available:false}` | pytest 全绿；fixture git 仓库实测 |
| **P5b** | backend 落库 `V004`（commit_stat / file_change_stat）+ `V007`（hotspot）+ FULL 流程接入（AnalysisRunner REPORT 前调用）+ `GET /projects/{id}/evolution?range=30d` 查询端点 | `mvn test` 全绿；聚合/降级分支测试 |
| **P5c** | 前端演化页：趋势折线 / TOP 文件条形 / 作者占比 + 热点卡片（风险中心）；非 Git / 无数据降级空态 | lint + build 全绿；无头浏览器冒烟（mock 契约数据） |

## 交付

| 变更 | 文件 |
|---|---|
| analyzer 演化模块 | `analyzer/app/core/evolution/`（gitlog.py / hotspot.py / schemas） |
| analyzer 路由 | `analyzer/app/main.py`（`/analyze/v1/evolution`） |
| 迁移 | `db/migration/V004__evolution.sql`、`V007__hotspot.sql`（V005/V006 属 P6/P7，编号占位不冲突） |
| 后端实体/Mapper/Service/Controller | `backend/src/main/java/com/evocode/...`（CommitStat/FileChangeStat/Hotspot + EvolutionController） |
| 前端演化页 | `frontend/src/views/project-detail/evolution-view.vue` + `api/evolution.ts` + 类型 |
| 记录 | `docs/devlog/2026-08-11-p5.md`、README 里程碑 + 版本历史 v2.9 |

## 关键决策

1. **非 Git 项目降级**：查询端点对非 Git 来源项目返回 **200 + `{available:false}`**（与 analyzer 05.6 契约一致，
   前端显示"非 Git 来源项目无演化数据"）。**偏离 06 §3.13 的 404/2001**（该错误码语义是"项目不存在"，用在
   "非 Git"上会造成前端无法区分"项目没了"与"没有 git 历史"）；沿用 P3a quality `available:false` 降级先例。
2. **热点双轨（规则先行，AI 可选）**：规则判定（按 commit_count / lines_added / 变更文件数阈值取 TOP-N，
   证据数组落库）→ 有 LLM key 时调 D.5 风险中心 Prompt 生成 `aiConclusion`（失败不阻塞，降级为规则结论）。
   复用 P2b 报告 LLM/RULES 双轨先例；hotspot.evidence 结构见 07 §5.6。
3. **git 统计实现**：subprocess 调系统 git（`git log --numstat --pretty=... --date=iso` 一次性取全量），
   **不引入 Python git 库**（保持 analyzer 依赖最小）；测试用临时 fixture git 仓库（本机 git 2.54 可用）。
4. **入库时机**：AnalysisRunner REPORT 阶段前调 `/analyze/v1/evolution`，按 analysis_id 落库
   commit_stat / file_change_stat / hotspot（失败降级：evolution 数据缺失不影响报告生成，同 P4b 架构降级）。
5. **不新增独立分析类型**：FULL 自动附带演化统计（与架构 P4b 一致）；`EVOLUTION` 类型枚举与单独触发留待后续。
6. **range 参数**：查询端点支持 `range=30d/90d/180d/all`（默认 30d），trend 按周聚合（契约 06 §3.13）。

## 待办（实施后回填）

- 验证：三端门禁 + fixture git 仓库实测 + 前端冒烟
- 踩坑记录
- P5 里程碑标记完成、版本历史 v2.9

---

## P5b 交付（2026-08-11）

**backend 落库 V004/V007 + FULL 流程接入 + `GET /projects/{id}/evolution` 查询端点**（feat: P5b）。

### 实现

| 文件 | 内容 |
|---|---|
| `db/migration/V004__evolution.sql` | commit_stat + file_change_stat（07 §3.9/3.10；补 analysis_id 维度 + UNIQUE(analysis_id, hash/path)，重跑先删后插） |
| `db/migration/V007__hotspot.sql` | hotspot（07 §3.16：module/risk_level/evidence jsonb/ai_conclusion） |
| `entity/` + `mapper/` | CommitStat / FileChangeStat / Hotspot + 三个 BaseMapper（CommitStatMapper 含 @Select 周聚合/作者聚合 SQL） |
| `dto/evolution/EvolutionResp.java` | 查询响应（available + commits/trend/topFiles/authors/hotspots，对齐 06 §3.13） |
| `service/EvolutionService(Impl)` | replaceForAnalysis（available=false 或 commits 空 → 跳过；否则先删后插）+ getForProject（range 30d/90d/180d/all，无数据 → available=false 而非 404） |
| `controller/EvolutionController` | `GET /api/v1/projects/{id}/evolution?range=30d` |
| `AnalyzerClient.evolution()` | 调 `/analyze/v1/evolution`（365d 窗口落库，查询再过滤），不可达 → 3001 |
| `AnalysisRunner.runEvolution()` | REPORT 前调用，失败降级不阻塞报告（同 P4b 架构） |
| `ProjectServiceImpl.delete()` | 级联删除 commit_stat / file_change_stat / hotspot |

### 关键决策落地

- **非 Git → 200 + available=false**（P5 规划决策 1；区别于 06 §3.13 的 404/2001 语义）
- **落库 365d 窗口，查询按 range 过滤**：commits/trend/authors 有时间过滤；topFiles/hotspots 取最新 analysis 全量（落库时已 365d）
- **ai_conclusion 占位**：hotspot 表已有列，v1.0 AI 医生填充（同 arch_violation.ai_note 先例）

### 验证

- `mvnw.cmd test` ✅ **82/82**（原 74 + 新增 6 EvolutionServiceImplTest + 2 AnalysisRunnerTest；AnalysisRunnerTest/ProjectServiceImplTest 构造器同步更新）
- init-db.ps1 按序扫描 `db/migration/V*.sql` → V004/V007 自动纳入幂等迁移

### 踩坑

1. **mock 断言与 Service 职责错位**：available=false 时 `replaceForAnalysis` 仍被 runner 调用（跳过逻辑在 Service 内部）→ 测试断言应为"被调用"而非 never。
2. **测试数据遗漏 committedAt**：mock selectList 返回的 CommitStat 未设时间 → `.toString()` NPE；服务端补空防御 + 测试数据补齐。

## 待办（P5c）

- 前端演化页：趋势折线 / TOP 文件条形 / 作者占比 + 热点卡片（风险中心）+ 非 Git/无数据降级空态

---

## P5c 交付（2026-08-11）——P5 收官

**前端演化页完成**（feat: P5c）：详情页新增"演化分析"区块，P5 三阶段全部落地。

### 实现

| 文件 | 内容 |
|---|---|
| `frontend/src/views/project-detail/evolution-view.vue` | 三 ECharts（提交趋势双轴折线 / TOP 变更文件横向条形 / 作者提交占比环形图）+ 风险中心热点卡片（riskLevel 徽标、evidence 证据列表、aiConclusion 展示）+ range 切换（30d/90d/180d/all）+ 加载/错误/非 Git 空态 |
| `frontend/src/api/evolution.ts` | `fetchEvolution(projectId, range)` → `GET /projects/{id}/evolution` |
| `frontend/src/types/api.ts` | EvolutionResult/Commit/Trend/TopFile/Author/Hotspot 类型 |
| `frontend/src/views/project-detail/index.vue` | 架构区块后接入 `<EvolutionView v-if="quality">` |

### 关键决策落地

- **回调 ref + `watch(flush:'post')`**：沿用 P4c 踩坑结论（字符串 ref 不回填 .value、nextTick 提前 resolve），三图容器全部回调 ref，未再踩坑
- **ECharts 按需引入**：Line/Bar/Pie + Grid/Tooltip/Legend + CanvasRenderer
- **热点卡三态**：HIGH 红左边框 / MEDIUM 琥珀 / aiConclusion 蓝色引用块

### 验证

- `npm run lint` ✅ + `npm run build` ✅（3.83s）
- **无头 Edge 真渲染冒烟**（临时 mock 契约数据，验证后清理）：
  - 场景 1（有数据）：3 个 canvas 全渲染（1082×240 + 2×522×240）；热点卡 2 张（HIGH 4 条证据 / MEDIUM 2 条证据 + aiConclusion）；range 4 按钮切换 active 正确；无 JS/ECharts 错误
  - 场景 2（available=false）：正确显示「该项目非 Git 来源或无提交历史，暂无演化数据。」，无图无卡

### 待办（P5 后）

- P5 真数据端到端补验（Docker 恢复后：起三端 + git 来源项目跑 FULL → 演化页实数据渲染）
- hotspot.ai_conclusion 由 v1.0 AI 医生填充（D.5 Prompt）
- 架构多版本对比（analysisId 切换，P4c 遗留）

---

## P5a 交付（2026-08-11）

**analyzer `/analyze/v1/evolution`** 完成并提交（feat: P5a）。

### 实现

| 文件 | 内容 |
|---|---|
| `analyzer/app/core/evolution/gitlog.py` | `git log --numstat`（subprocess 调系统 git，不引入 Python git 库）→ commit 解析（\x1f 分隔防空格歧义、`core.quotepath=false` 防路径转义）→ trend（按 ISO 周聚合）/ topFiles / authors 聚合 + 规则热点判定 |
| `analyzer/app/core/evolution/__init__.py` | `evolution_scan(git_dir, range_days)` 主入口：非 git 目录 → `{available:false}`（契约 05.6） |
| `analyzer/app/schemas.py` | `EvolutionRequest/Commit/Trend/TopFile/Author/Hotspot/Result`（对齐 06 §5.6） |
| `analyzer/app/main.py` | `POST /analyze/v1/evolution` 路由（gitDir 不存在 → 404） |
| `analyzer/tests/test_evolution.py` | 10 例：非 git / commits 字段 / 周聚合 / TOP 文件 / 作者 / 热点 HIGH+MEDIUM / range 过滤 / 端点 200（非 git + 正常）/ 404 |

### 热点规则（P5 关键决策 2 落地）

- HIGH：变更占比 ≥ 15% 且新增 ≥ 300 行，或新增 ≥ 2000 行
- MEDIUM：变更 ≥ 3 次
- evidence 数组：`["变更 N 次", "新增 X 行", "删除 Y 行", "占全部提交的 Z%"]`（07 §5.6）
- ai_conclusion 由 backend 侧（P5b）可选调 D.5 Prompt 补充，analyzer 不阻塞

### 验证

- `ruff check .` ✅ All checks passed
- `pytest -q` ✅ **85/85**（原 75 + 新增 10）
- **真实仓库冒烟**：对 evocode 自身 git 仓库（range 365d）→ available=true、commits=4、trend 1 周、
  topFiles= package-lock.json / index.vue / types/api.ts、hotspots= package-lock.json + index.vue 均 HIGH（变更 50% + 千行级）——规则判定符合直觉

### 踩坑

1. **测试周聚合用相对天数跨周**：10/8 天前可能落在周末两侧 → 断言周数不稳。改用**绝对 ISO 日期**构造同周数据（07-20 周一 + 07-21 周二 = 同周）。
2. **`datetime.UTC` 是模块级别名**：`from datetime import datetime` 后 `datetime` 是类，`datetime.UTC` 报 AttributeError；应 `from datetime import UTC`。ruff UP017 的提示按 `import datetime` 语境，易误导。
3. **git numstat 行匹配**：内容只有少量行变化时 diff 只计变化行 → linesAdded 偏小、HIGH 阈值达不到。测试 fixture 每行含唯一序号保证全量 diff。
