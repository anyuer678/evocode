# P7 · v0.6 技术债与文档生成 规划

> 日期：2026-08 ｜ 状态：规划已确认，待实施
> 契约依据：`docs/06-API契约.md` §3.12（tech_debt 列表/状态机）、§3.14（docs 列表/编辑）；`docs/07-数据字典.md` §3.11/§3.12；`docs/02-开发指导.md` §11 任务 ① ②、附录 D.7（三类文档 prompt）
> 前置：V005（tech_debt/generated_doc）表已在 P6a 建好，功能待启用

## 目标

v0.6：① **技术债闭环**——分析产物（架构/质量/依赖/演化）自动落 tech_debt，前端可查看并按状态机流转（OPEN→DOING→DONE / WONTFIX）；② **三类文档生成**——README / 架构文档 / API 文档（D.7 prompt），支持查看、人工编辑（version 递增 + edited 标记）、重新生成。

## 交付

| 端 | 内容 |
|---|---|
| analyzer | `POST /analyze/v1/doc`（契约 §5 新增）：{projectId, docType} → 200 {docType, title, content}；D.7 三类 prompt；输入 = scan 摘要 + 架构 + API 列表（从已落库分析产物）；LLM 失败 → 4xx 明确错误（文档无法规则降级） |
| backend | tech_debt：分析完成后从已落库产物聚合生成（ARCH=架构违规 HIGH/MEDIUM、QUALITY=质量 BLOCKER/CRITICAL、DEPEND=EOL 依赖、EVOLUTION=热点 HIGH），同 analysis 幂等重建；`GET /projects/{id}/tech-debts?status=`（分页）+ `POST /tech-debts/{id}/status`（状态机严格校验，非法迁移 2011）；docs：`GET /projects/{id}/docs?type=`、`POST /projects/{id}/docs/generate`（调 analyzer 落库，edited 文档重新生成需确认）、`POST /docs/{id}/edit`（version+1、edited=true） |
| frontend | 详情页新增"技术债"区块（列表 + 严重级徽标 + 来源标签 + 状态操作下拉 + 备注输入）；新增"文档"区块（README/架构/API 三 tab + 生成/重新生成 + Markdown 渲染 + 编辑模式）；路由/入口并入详情页 |
| db | V005 已建（无新迁移）；删除项目级联已在 §3.4 时序覆盖 |

## 关键决策（AD）

- **AD-P7-1 tech_debt 聚合在 backend**：从已落库的 arch_violation / quality_issue / 依赖 / hotspot 提取（`ref_analysis_id` 关联，同 analysis 重建：重新分析时先删同 analysis 旧债再插），与 analysis 状态联动，不进 analyzer。**AI_DOCTOR / MANUAL 来源本期不做**（会话解析复杂度高、契约无手动新建接口），记遗留。
- **AD-P7-2 文档生成同步**：backend → analyzer 同步调用（LLM 生成 ≤30s，前端 loading 态）；无 LLM key → 明确报错（400/LLM_NO_KEY 语义），文档无法规则降级。
- **AD-P7-3 编辑保护**：`edited=true` 的文档重新生成需前端二次确认（生成新 version 覆盖）；编辑接口 version 单调递增。
- **AD-P7-4 状态机严格按契约**：OPEN→DOING / OPEN→DONE / OPEN→WONTFIX / DOING→DONE；非法迁移 → 新错误码 **2011**（DEBT_STATUS_INVALID）；DONE 必填 resolveNote、WONTFIX 必填 wonfixReason（1001 缺失校验）。
- **AD-P7-5 文档类型枚举**：README / ARCH / API（`doc_type` 白名单，非法 → 1002）；每项目每类型单文档（upsert 语义，generate 更新同类型记录）。

## 任务拆分

| 步骤 | 内容 | 验收 |
|---|---|---|
| P7a | backend tech_debt 聚合（AnalysisRunner 完成后）+ 列表/状态接口 + 2011 错误码；前端技术债区块 | backend 测试（聚合规则/状态机/校验）全绿；前端 lint/build |
| P7b | analyzer `/analyze/v1/doc`（D.7 三类 prompt）+ backend docs 生成/编辑接口 + 前端文档区块（三 tab + 编辑 + 重新生成确认） | analyzer 测试（三类生成/LLM 失败语义）+ backend 测试全绿；前端 lint/build |
| P7c | 三端回归 + 真实 HTTP 冒烟 + devlog 交付段 + README 版本历史 + 提交推送 | 回归全绿；冒烟（doc 生成降级语义） |

## 风险与降级

- Docker/PG 不可用 → 真库验证留待恢复（沿用 P6 惯例，单测 + mock + 真实 HTTP 冒烟）
- LLM 无 key → doc 生成明确报错（不做规则版伪文档）；tech_debt 聚合不依赖 LLM（规则提取），无 key 也可用
- 架构/质量/演化数据缺失（对应分析未跑）→ 对应来源的债为空（不报错）

## 遗留增强（记 devlog，不阻塞）

- AI_DOCTOR 来源（医生会话识别技术债自动落库，需解析对话）
- MANUAL 手动新建（契约无接口，后续补 POST /tech-debts）
- Dashboard 与全面美化 + 深浅色（02 §11 任务 ③④，P7a/P7b 后单独规划）

## P7a 交付（待提交）

### backend：tech_debt 聚合 + 接口

| 文件 | 内容 |
|---|---|
| `entity/TechDebt` + `mapper/TechDebtMapper` | V005 表实体（@Data + @Accessors(chain)），Mapper 空接口 |
| `service/debt/TechDebtServiceImpl` | `rebuildForAnalysis`（AD-P7-1 四源聚合，同 analysis 先删后插幂等）：**ARCH**=arch_violation HIGH/MEDIUM（title=类型+描述截 60）；**QUALITY**=quality_issue BLOCKER/CRITICAL（BLOCKER→HIGH、CRITICAL→MEDIUM）；**EVOLUTION**=hotspot HIGH（module 热点）；**DEPEND**=report_json.risks 中 title 含 EOL/依赖/版本/升级（dependency 清单从未落库，P3 只做 quality，故从报告风险提取） |
| `common/ErrorCode` | 新增 **2011** DEBT_STATUS_INVALID（非法迁移/必填缺失）、**2012** DEBT_NOT_FOUND |
| `controller/DebtController` | GET `/projects/{id}/tech-debts`（分页 + status 筛选）、POST `/tech-debts/{id}/status` |
| `service/analysis/AnalysisRunner` | report 落库后调 `rebuildForAnalysis`（失败仅告警不阻塞分析）；构造器 + TechDebtService |
| 状态机 | OPEN→DOING/DONE/WONTFIX、DOING→DONE（06 §3.12）；DONE 必填 resolveNote、WONTFIX 必填 wonfixReason → 2011；修复了初版 TRANSITIONS 映射方向用反的 bug |
| 测试 | TechDebtServiceImplTest 11 例（列表筛选/2001/2012/状态机/必填/四源聚合/CRITICAL→MEDIUM 映射）；AnalysisRunnerTest 同步构造器 |

### frontend：技术债区块

| 文件 | 内容 |
|---|---|
| `api/debt.ts` | fetchTechDebts（分页+status 筛选）/ updateTechDebtStatus（DONE/WONTFIX 备注） |
| `types/api.ts` | TechDebtItem（source/level/status 枚举） |
| `views/project-detail/tech-debt-view.vue` | 列表（level 徽标红/黄/蓝 + source 标签 + status 徽标 + 处理按钮）+ 状态筛选 + 处理弹层（DOING/DONE/WONTFIX + 必填校验前端拦截） |
| `views/project-detail/index.vue` | AI 医生区块后接入 `<TechDebtView v-if="detail?.status === 'READY'" />` |

### 验证

- backend **108/108**（97 + 11）BUILD SUCCESS
- frontend lint + build 全绿（踩坑：prettier 把 `@click="a; b()"` 拆多行 → Vue 模板多语句解析失败 → 改单一方法 `onFilter`）
- 真实链路（分析 → 技术债生成 → 状态流转）待 Docker 恢复后端到端

## P7b 交付（待提交）

### analyzer：/analyze/v1/doc（06 §5.9 契约新增）

| 文件 | 内容 |
|---|---|
| `core/docgen.py` | D.7 三类 prompt（README 项目简介/技术栈/目录/快速开始/运行要求；ARCH 模块/分层/调用流程/部署 ASCII；API 端点表格）；`_extract_controllers` 正则解析（@RestController 类 + 类级 @RequestMapping 前缀 + 方法级映射 + 方法签名），非 controller 忽略；输入截断保护（controller 4000 行、scan files 200、nodes 100/edges 200） |
| `schemas.py` | DocRequest（projectId/docType/scan/arch/projectInfo/codeDir）+ DocResponse |
| `main.py` | POST /analyze/v1/doc：docType 白名单 400；LLM_NO_KEY → 400、LLM_FAILED → 502（detail 带 code） |
| `tests/test_doc.py` | 9 例：端点提取（类前缀拼接）/非 controller 忽略/目录缺失/三类生成/非法类型/路由降级/成功 |

### backend：docs 接口

| 文件 | 内容 |
|---|---|
| `entity/GeneratedDoc` + `mapper/GeneratedDocMapper` | V005 表实体（version/edited） |
| `service/doc/DocServiceImpl` | list（三类不分页）/ generate（同步调 analyzer，组装 scan=file_node 重建 + arch=最新 analysis 架构 + codeDir=storagePath；同类型 upsert version 递增，AD-P7-3 edited 前端确认）/ edit（version+1、edited=true，2013） |
| `service/analysis/AnalyzerClient` | `doc()` + DocRequest/DocResp record（非 2xx → 3001，LLM_NO_KEY/LLM_FAILED 统一映射） |
| `common/ErrorCode` | 2013 DOC_NOT_FOUND |
| `controller/DocController` | GET `/projects/{id}/docs`、POST `/projects/{id}/docs/{docType}/generate`、POST `/docs/{id}/edit` |
| 测试 | DocServiceImplTest 8 例（列表/非法类型 1002/3001/新建 v1/已有递增/编辑 2013/version+1+edited） |

### frontend：文档区块

- `api/doc.ts`（fetchDocs/generateDoc/editDoc）+ `types/api.ts`（DocItem/DocType）
- `doc-view.vue`：三 tab + 轻量安全 Markdown 渲染（标题/代码块/表格/列表/粗体/行内码，先转义防 XSS）+ 生成（loading 提示 10-30s）+ 重新生成（edited 二次确认）+ 编辑模式
- `index.vue` 接入（技术债区块后）

### 验证

- analyzer **116/116**（107 + 9）+ ruff 全绿
- backend **116/116**（108 + 8）BUILD SUCCESS
- frontend lint + build 全绿
- 真实生成（LLM + 项目数据）待 Docker/LLM 环境恢复后端到端

## P7 收官（待提交）

### 三端回归（P7 门禁）

| 端 | 结果 |
|---|---|
| analyzer | pytest **116/116**（P7b +9 doc）+ ruff All checks passed |
| backend | mvn **116/116**（P7a +11 debt、P7b +8 doc）BUILD SUCCESS |
| frontend | lint（eslint + prettier）全绿 + `npm run build`（vue-tsc + vite）成功 |

### 真实 HTTP 冒烟（analyzer 单端，无 LLM/PG）

起真实 uvicorn（127.0.0.1:8081）后：

```
POST /analyze/v1/doc {"docType":"PDF"}          → 400 {"detail":"docType 必须是 README/ARCH/API"}
POST /analyze/v1/doc {"docType":"README"}（无 key）→ 400 {"detail":{"code":"LLM_NO_KEY",
                                                             "message":"文档生成失败：LLM_API_KEY 未配置（LLM_NO_KEY）"}}
```

- 验证 docType 白名单与 LLM_NO_KEY 降级语义（detail 携带 code 供前端分支）
- 真实 LLM 生成（README/架构/API 内容）待 LLM 环境恢复后端到端

### 端到端待办（Docker/LLM 恢复后）

1. `start-dev.bat` 三服务拉起（init-db 自动跑 V005/V006）
2. 真实项目 FULL 分析 → 技术债四源自动生成（查 tech_debt 行数）
3. 技术债状态流转（前端 DOING/DONE/WONTFIX）
4. 三类文档生成（README/架构/API）→ 前端渲染 + 编辑 + 重新生成确认
5. AI 医生对话引用（P6 待办同步验证）

### 记录

- README 版本历史 v3.9（P7 收官）；P7 提交链：eb5b7c3（规划）→ 3e2137b（P7a）→ 0870f6c（P7b）→ 本提交
- 遗留增强：AI_DOCTOR/MANUAL 技术债来源、依赖清单落库（DEPEND 暂从报告风险提取）、Dashboard 与全面美化 + 深浅色（02 §11 任务 ③④，单独规划）
