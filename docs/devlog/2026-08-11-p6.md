# P6 · v0.5 AI 医生（RAG 问答带引用）规划

> 日期：2026-08-11 ｜ 状态：规划已确认，待实施
> 契约依据：`docs/06-API契约.md` §5.7（`POST /analyze/v1/chat` SSE 生成端）、§5.8（`/analyze/v1/rag/index` · `/analyze/v1/rag/search`）；`docs/07-数据字典.md` V005（tech_debt/generated_doc）、V006（chat_session/chat_message/knowledge_chunk）
> 设计依据：`docs/02-开发指导.md` §9（切片/检索/引用/流式）、附录 D.6（AI 医生系统提示词）、E.4（对话样例）

## 目标

项目详情页提供 **AI 医生**：基于分析产物的 RAG 问答，流式回复并附**可点击的代码引用**（文件/行号），支持文件预览（Monaco 按需加载）。交付物：AI 医生问答带引用，版本 v0.5。

## 交付

| 端 | 内容 |
|---|---|
| db | `V005__tech_doc.sql`（tech_debt / generated_doc，表结构先行，功能留 P7）＋ `V006__chat_rag.sql`（chat_session / chat_message / knowledge_chunk，knowledge_chunk 带 `vector(1024)` + HNSW 索引） |
| analyzer | 切片（tree-sitter 符号提取，≤800 token/块，超长 400/overlap 50 滑切）；`LLMClient.embed()`（OpenAI 兼容 `/embeddings`，模型默认 `bge-m3`）；PG 直连写/查 knowledge_chunk（随 analysis 全量重建）；`POST /analyze/v1/rag/index`、`POST /analyze/v1/rag/search`（向量 cosine top8 + 关键词 LIKE top4 合并去重 → topK=8）；`POST /analyze/v1/chat` SSE 生成端（delta/citations/done/error） |
| backend | chat_session / chat_message CRUD（V006 落库，含 citations jsonb）；`POST /api/v1/projects/{id}/chats/{id}/messages` → SSE 透传 analyzer（SseEmitter，不缓冲） |
| frontend | AI 医生对话页（会话列表 + 流式渲染 + 引用卡片 + Monaco 预览按需加载） |

## 关键决策（AD）

- **AD-P6-1 embedding 来源**：复用现有 `LLM_BASE_URL`/`LLM_API_KEY` 调 `/embeddings`，模型 `LLM_EMBEDDING_MODEL` 默认 `bge-m3`（1024 维，对齐 `vector(1024)`）。未配置 key → `embedding` 列留空，检索降级纯关键词（文件路径/符号 LIKE），配置好后再启用向量（与 LLM_NO_KEY 降级哲学一致）。
- **AD-P6-2 数据库边界**：analyzer 直连 PG（`ANALYZER_PG_DSN`，psycopg3），**只读写 knowledge_chunk 一张表**；切片+embedding+入库全在 analyzer（契约 §5.8 已定"直读磁盘不经 HTTP 传文件"），避免大 payload 往返。其余表仍归 backend。
- **AD-P6-3 切片单元**：复用 P4b 架构模块（`analyzer/app/core/arch/*.py`）的 tree-sitter 符号提取（函数/类/模块），`meta = {symbol, lang}`；超长函数 400 token 窗口滑切、overlap 50；`analysis_id` 随分析全量重建（07 字典定义）。
- **AD-P6-4 会话标题**：不额外发 LLM 请求，取首条 query 截断 ≤20 字作 title（规则版；AI 生成留待 P7 增强）。
- **AD-P6-5 引用防幻觉**：prompt 强约束只引用检索集合内文件（`[path:line]`）；输出后校验 citations ∈ 检索集合，否则剔除；检索为空 → 兜底话术"该问题超出当前分析范围，建议发起一次新分析"。
- **AD-P6-6 流式协议**：事件 `delta / citations / done / error`，协议与 06 §4 一致；backend→analyzer 同结构 SSE 透传不缓冲；history 截断 ≤6 轮 + 早期摘要。

## 任务拆分

| 步骤 | 内容 | 验收 |
|---|---|---|
| P6a | V005/V006 迁移；analyzer：切片模块 + `embed()` + PG 直连 + `/rag/index` + `/rag/search` + 分析成功顺带索引 | pytest 全绿；index/search 单测（含无 key 关键词兜底、去重、topK） |
| P6b | analyzer `/analyze/v1/chat` SSE 生成端（D.6 prompt、温度 0.7、引用校验）；backend 会话/消息 CRUD + SSE 透传 | analyzer pytest（SSE 事件流、截断、引用剔除）；backend 83+ 例全绿 |
| P6c | 前端 AI 医生页：会话列表 + 对话 + SSE 流式 + 引用卡片 + Monaco 按需加载 | lint + build 全绿；组件单测 |
| P6d | 端到端冒烟（三服务拉起 + 真实项目问答）+ devlog 交付段 + README 版本历史 + 提交推送 | 三端回归全绿；提交 `feat: P6 ...` 推送 |

## 风险与降级

- Docker/PG 不可用 → 无法真库验证，用单测 + mock 覆盖（沿用 P4/P5 惯例）
- embedding 网关无 bge-m3 → `LLM_EMBEDDING_MODEL` 覆盖为可用模型，维度变化时同步 `vector(n)`（迁移文件以 bge-m3 1024 为默认）
- LLM 无 key → chat 返回 error 事件带 LLM_NO_KEY 语义，前端提示配置；RAG 检索仍可用（关键词）

## P6a 交付（待提交）

### 交付物

| 端 | 内容 |
|---|---|
| db | `V005__tech_doc.sql`（tech_debt/generated_doc 建表，功能留 P7）＋ `V006__chat_rag.sql`（chat_session/chat_message/knowledge_chunk，`vector(1024)` + HNSW cosine 索引） |
| analyzer | `core/rag/chunker.py`（tree-sitter 符号切片，≤800 token/块，超长 1600 chars 滑切 overlap 200；未支持语言整文件兜底）；`core/rag/vectorstore.py`（PG 直连，项目级全量重建 + 向量/关键词合并检索）；`core/rag/service.py`（编排 + embedding 降级）；`llm.py` 加 `embed()`（/embeddings，默认 bge-m3）；config 加 `llm_embedding_model`/`pg_dsn`；schemas + `POST /analyze/v1/rag/index` + `/analyze/v1/rag/search` |
| 测试 | `tests/test_rag.py` 11 例：符号切片/滑切/兜底/模块剩余区/keywords（中英/停用词/去重限流）/路由降级（search 无 PG→503、index 目录缺失→404、index 无 PG→200 stored=false） |

### 关键实现注

- **analysis_id 可空**：analyzer 无 analysis 概念（backend 管）；`index` 入参加可选 `analysisId`，backend 链路传、手动索引为 NULL；rebuild 按 **project 级全量重建**（DELETE WHERE project_id），手动索引同样幂等
- **检索合并**：向量 cosine topK（查询 embedding 可用时）＋ 关键词 LIKE（`file_path`/`content` 各匹配任一关键词，`%k%`）→ `(file, chunkIndex)` 去重 → score 降序 topK；修了 SQL 占位符与 params 数量不匹配的初版 bug
- **降级链**：无 LLM key / embedding 网关失败 → embedding 列 NULL + 纯关键词；PG 未配置 → `search` 503、`index` 200 `stored=false`（切片仍产出）

### 验证

- analyzer pytest **101/101**（原 90 + 新增 11）全绿
- ruff check（E501/SIM117/UP015/I001 共 16 项）修复后 **All checks passed**
- 真实 PG 验证留待 Docker 恢复（P6d 端到端）

## P6b 交付（待提交）

### analyzer：chat 生成端（06 §5.7 / §4 SSE）

| 文件 | 内容 |
|---|---|
| `core/prompts.py` | `DOCTOR_SYSTEM_TEMPLATE`（附录 D.6：只引用背景资料 [path:line]、先结论后证据、范围外明确说明）+ `build_doctor_prompt`（fileRef 全文发送截断 4000 字符） |
| `core/llm.py` | `chat_stream()`：`/chat/completions` stream=true 逐 delta yield（SSE `data:` 行解析，`[DONE]` 结束），温度 0.7；`_parse_stream_delta` 抽取 |
| `core/rag/service.py` | `chat()` 生成器：检索（向量/关键词降级）→ 无结果兜底话术（不调 LLM）→ D.6 prompt → 流式 → `_extract_citations` 引用校验（正则 `[path:line]`，剔除不在检索集合 / 行号超出 chunk 区间）→ citations/done；error 事件带 `LLM_NO_KEY`/`LLM_FAILED` |
| `core/rag/chunker.py` | `CodeChunk` 加 `start_line/end_line`（符号切片行号区间；模块级/兜底为全文件），meta 同步 startLine/endLine |
| `main.py` | `POST /analyze/v1/chat` → `StreamingResponse`（text/event-stream + X-Accel-Buffering: no） |
| `tests/test_chat.py` | 6 例：引用保留/剔除/区间外/basename；SSE 兜底话术、error 事件、delta→citations→done 全流 |

### backend：会话 CRUD + SSE 透传（06 §3.15 / §4）

| 文件 | 内容 |
|---|---|
| `entity/ChatSession` / `ChatMessage` | V006 表实体（createdAt fill + @TableLogic deleted；citations jsonb typeHandler） |
| `mapper/ChatSessionMapper` / `ChatMessageMapper` | BaseMapper 空接口 |
| `dto/chat/*` | ChatListResp（{total, items} 不分页）、ChatCreateResp、ChatMessageListResp、ChatSendReq 等 record |
| `service/chat/ChatServiceImpl` | 列表（lastMessageAt desc 排序 + messageCount）、建会话（title 占位"新会话"）、删会话（@Transactional 级联逻辑删）、消息分页、发消息（同 content 2s 防重复 2007 + user 落库 + 标题自动生成 query 前 20 字） |
| `service/chat/ChatStreamer` | 独立 bean + `@Async("chatExecutor")`：JDK HttpClient 流式读 analyzer SSE → SseEmitter 逐事件转发（delta/citations/done/error）→ done 落库 assistant（content + citations jsonb）并回填 messageId；error 分支落库失败消息；空响应/异常同样落库 + error 事件 |
| `service/analysis/AnalyzerClient` | `chatStream()` 返回 BufferedReader（java.net.http.HttpClient，180s 超时，非 200 → 3001）；构造器加 ObjectMapper |
| `config/AsyncConfig` | 新增 `chatExecutor`（core 2 / max 4，SSE 长连接专用池） |
| `controller/ChatController` | GET/POST `/projects/{id}/chats`、DELETE `/chats/{id}`、GET `/chats/{id}/messages`（分页校验 1003）、POST `/chats/{id}/messages`（SSE 180s） |
| 测试 | ChatServiceImplTest 9 例 + ChatStreamerTest 5 例（mock analyzer 流 / SseEmitter，含 error/异常/空响应分支） |

### 关键实现注

- **SSE 透传不缓冲**：`SseEmitter.event().name().data()` 逐事件转发；analyzer 的 done 无 messageId，backend 落库后自发送 `{"messageId": id}`
- **error 双重处理修复**：relay 遇 analyzer error 事件即落库 + 转发 + complete，`RelayResult.handled` 标记阻止空 answer 分支重复处理
- **引用落库**：citations 事件在 done 前到达，relay 收集后随 assistant 消息一并落库（07 §5.5）
- **防目录穿越**：fileRef 解析 `resolve().normalize()` 后 `startsWith(base)` 校验
- **标题生成**：规则版 query 前 20 字（AD-P6-4，AI 生成留 P7）

### 验证

- analyzer pytest **107/107**（+6 chat 用例）+ ruff All checks passed
- backend **97/97**（+16 chat 用例）BUILD SUCCESS；AnalyzerClient 构造器变更同步现有测试
- README 版本历史：日期统一为 `2026-08`（不精确到天）；新增 v3.3（P6a）/ v3.4（P6b）；devlog 引用路径补全日期（18 处）
- 真实链路（PG + LLM + 前端）留待 P6c/P6d 端到端

