[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
# 03 §4.2：规则全开（仅 line-length 例外）
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
